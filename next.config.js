/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'th.bing.com',
            'assets5.lottiefiles.com'
        ]
    },
}

module.exports = nextConfig
